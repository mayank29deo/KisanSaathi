import React, { useState, useMemo } from "react";
import Card from "../components/Card";
import STATEROTATIONS from "../constants/stateRotations";

export default function Rotation({ t }) {
  // ...state/filter logic
  return (
    <div>
      {/* Crop rotation library, filters */}
      <Card>...</Card>
    </div>
  );
}
